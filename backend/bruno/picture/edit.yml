info:
  name: edit
  type: http
  seq: 4

http:
  method: PUT
  url: http://localhost:3000/picture/3
  body:
    type: json
    data: |-
      {
      "latitude":20,
      "longitude":110,
      "path":"testx",
      "commentary":"test-commentaryx"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
