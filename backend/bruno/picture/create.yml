info:
  name: create
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:3000/picture
  body:
    type: json
    data: |-
      {
      "latitude":200,
      "longitude":100,
      "path":"test",
      "commentary":"test-commentary"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
